# Architecture Overview
*AIgnition 3.0 — Probabilistic Revenue Forecasting*

> Stub — Ipshita completes this on Day 14.

## Sections to fill in:
- Frontend stack (React + Recharts + Tailwind)
- Backend/pipeline stack (Python 3.11, pandas, Prophet, XGBoost, Ridge)
- Forecasting pipeline flow diagram
- LLM integration workflow (Claude Roles A/B/C, UI-only)
- Data flow: CSV → parquet → pickle → predictions.csv → UI
